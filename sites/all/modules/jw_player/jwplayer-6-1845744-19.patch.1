diff --git a/jw_player.admin.inc b/jw_player.admin.inc
index de62218..3305919 100644
--- a/jw_player.admin.inc
+++ b/jw_player.admin.inc
@@ -12,6 +12,13 @@ function jw_player_settings_form($form) {
 
   drupal_set_title(t('JW Player general settings'));
 
+  $form['jw_player_version'] = array(
+    '#type' => 'select',
+    '#title' => t('JW Player Version'),
+    '#options' => drupal_map_assoc(array(5, 6)),
+    '#default_value' => variable_get('jw_player_version', 6),
+  );
+
   $form['jw_player_inline_js'] = array(
     '#type' => 'checkbox',
     '#title' => t('Use inline javascript'),
diff --git a/jw_player.install b/jw_player.install
index c66bb0b..f09bcab 100644
--- a/jw_player.install
+++ b/jw_player.install
@@ -54,6 +54,14 @@ function jw_player_schema() {
 }
 
 /**
+ * Set jw player version to 5, display message.
+ */
+function jw_player_update_7000() {
+  variable_set('jw_player_version', 5);
+  drupal_set_message(t('JW Player version has been set to 5.'));
+}
+
+/**
  * Implements hook_requirements().
  */
 function jw_player_requirements($phase) {
@@ -69,7 +77,15 @@ function jw_player_requirements($phase) {
   if ($phase == 'runtime') {
     $errors = array();
 
-    foreach (array('player.swf', 'jwplayer.js') as $file) {
+    $files = array('jwplayer.js');
+    if (variable_get('jw_player_version', 6) == 6) {
+      $files[] = 'jwplayer.html5.js';
+      $files[] = 'jwplayer.flash.swf';
+    }
+    else {
+      $files[] = 'player.swf';
+    }
+    foreach ($files as $file) {
       if (!file_exists($directory . '/' . $file)) {
         $errors[] = $t('The file %file is not present in the directory %directory', array('%file' => $file, '%directory' => $directory));
       }
diff --git a/jw_player.module b/jw_player.module
index 7799758..c4d5743 100644
--- a/jw_player.module
+++ b/jw_player.module
@@ -240,6 +240,21 @@ function jw_player_preset_load($machine_name = NULL) {
 }
 
 /**
+ * Returns the flashplayer filename based on the JW Player version.
+ *
+ * @return
+ *   jwplayer.flash.swf for version 6, player.swf for version 5.
+ */
+function jw_player_flashplayer() {
+  if (variable_get('jw_player_version', 6) == 6) {
+    return 'jwplayer.flash.swf';
+  }
+  else  {
+    return 'player.swf';
+  }
+}
+
+/**
  * Returns the default settings for JW Player. Used in cases where a preset is
  * not provided when the JW Player theme function is called.
  */
@@ -247,8 +262,9 @@ function jw_player_default_settings() {
   $defaults = &drupal_static(__FUNCTION__);
 
   if (!isset($defaults)) {
+    $flash_filename =
     $defaults = array(
-      'flashplayer' => file_create_url(libraries_get_path('jwplayer') . '/player.swf'),
+      'flashplayer' => file_create_url(libraries_get_path('jwplayer') . '/' . jw_player_flashplayer()),
       'width' => '640',
       'height' => '480',
       'autoplay' => FALSE,
@@ -399,7 +415,7 @@ function template_preprocess_jw_player(&$variables) {
     ),
     array(
       'type' => 'flash',
-      'src' => file_create_url(libraries_get_path('jwplayer') . '/player.swf'),
+      'src' => file_create_url(libraries_get_path('jwplayer') . '/' . jw_player_flashplayer()),
     ),
   );
 
@@ -443,6 +459,13 @@ function jw_player_process_jw_player(&$variables) {
   else {
     // Add library.
     drupal_add_js(libraries_get_path('jwplayer') . '/jwplayer.js');
+    // Compatibility with new version
+    if (variable_get('jw_player_version', 6) == 6) {
+      // @todo: The html5 js file needs to be added because it's not found when
+      //   aggregation is enabled. Update once
+      //   http://developer.longtailvideo.com/trac/ticket/1833 is fixed.
+      drupal_add_js(libraries_get_path('jwplayer') . '/jwplayer.html5.js');
+    }
     // Attaches JW Player element.
     drupal_add_js(drupal_get_path('module', 'jw_player') . '/jw_player.js');
     drupal_add_js(array('jw_player' => array($variables['html_id'] => $variables['config'])), 'setting'); // Player settings
@@ -468,6 +491,12 @@ function jw_player_init() {
   if (variable_get('jw_player_inline_js', FALSE)) {
     // Add library
     drupal_add_js(libraries_get_path('jwplayer') . '/jwplayer.js');
+    if (variable_get('jw_player_version', 6) == 6) {
+      // @todo: The html5 js file needs to be added because it's not found when
+      //   aggregation is enabled. Update once
+      //   http://developer.longtailvideo.com/trac/ticket/1833 is fixed.
+      drupal_add_js(libraries_get_path('jwplayer') . '/jwplayer.html5.js');
+    }
 
     // Add key if available.
     if ($key = variable_get('jw_player_key', NULL)) {
